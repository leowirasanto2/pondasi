/// Hardcoded feature flags. Future: replace with Firebase Remote Config values.
enum FeatureFlags {
    /// Controls visibility of the voice recording feature and its entry points.
    static let isVoiceEnabled: Bool = false
}
